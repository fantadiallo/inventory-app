import LogHistory from "../../components/LogHistory/LogHistory";

/**
 * Page for displaying all approved inventory logs.
 * @component
 * @returns {JSX.Element}
 */
export default function LogHistoryPage() {
  return (
    <main>
      <LogHistory />
    </main>
  );
}
